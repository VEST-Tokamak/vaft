% Presenter build only. Beamer hides \note{} content by default, which is what
% the archival PDF wants; this turns it back on so the same notes become facing
% pages. Never included by the default beamer format.
\setbeameroption{show notes}
